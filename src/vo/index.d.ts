import { Rule } from '@angular-devkit/schematics';
import { Schema as VoOptions } from './schema';
/**
 * Creates a Value Object.
 * @param {Schema} options
 * @returns {Rule}
 */
export declare function vo(options: VoOptions): Rule;
